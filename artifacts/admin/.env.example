# ─── Admin dashboard — environment variables ──────────────────────────────────
# In Vercel: Project → Settings → Environment Variables.
# Prefix VITE_ is required — Vite only exposes variables with this prefix.

# Full URL of the deployed API server (no trailing slash).
# Example: https://centralstudio-api.up.railway.app
VITE_API_URL=https://your-api.up.railway.app

# No API key variable is needed — the admin dashboard authenticates with an
# admin JWT (obtained via login), sent as X-Admin-Token. There is no shared
# server-to-server secret in this system.
